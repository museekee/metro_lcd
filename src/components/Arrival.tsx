import { useEffect } from "react"

const Arrival = ({
  line,stationCode
}: {
  line: Lines, stationCode: string
}) => {
  // 이번역: 위에서 내려 옴
  // 객실 칸에 따른 역 정보: 밑에서 올라옴
  useEffect(() => {
    console.log("Nigger")
  }, [])
  return (
    <></>
  )
}

export default Arrival